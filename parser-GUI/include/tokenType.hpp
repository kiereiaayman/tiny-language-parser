#pragma once

enum class TokenType
{
    IF,
    THEN,
    ELSE,
    END,
    REPEAT,
    UNTIL,
    READ,
    WRITE,
    IDENTIFIER,
    NUMBER,
    ASSIGN,
    LESSTHAN,
    EQUAL,
    PLUS,
    MINUS,
    MULT,
    DIV,
    OPENBRACKET,
    CLOSEDBRACKET,
    SEMICOLON,
    ERROR,
    ENDFILE
};